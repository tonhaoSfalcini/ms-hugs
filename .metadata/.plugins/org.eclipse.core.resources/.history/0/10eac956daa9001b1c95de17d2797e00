package com.hugs.services;

import org.springframework.stereotype.Service;

import com.hugs.dto.CidadeIbgeDTO;
import com.hugs.dto.ClienteDTO;
import com.hugs.entities.Cidade;
import com.hugs.entities.Cliente;

@Service
public class CidadeService {
	
	public Cidade convertDtoToEntity(CidadeIbgeDTO dto) {
		Cidade cidade = new Cidade(dto.getId(), dto.getNome(), dto.getMicrorregiao().getMesorregiao().get)	
		
		return cidade;
	}
	
	public CidadeIbgeDTO  convertEntityToDto(Cidade cidade) {
		CidadeIbgeDTO dto = new ClienteDTO(
//										cliente.getId(), 
										cidade.getCpfCnpj(), 
										cidade.getNomeRazaoSocial(),
										cidade.getTipoPessoa(),
										cidade.getDataNascimentoConstituicao(),
										cidade.getEmail(),
										cidade.getWhatsApp(),
										cidade.getTelefone_1(),
										cidade.getTelefone_2(),
										cidade.getTelefone_3(),
										cidade.getPermiteMktWhatsapp(),
										cidade.getPermiteMktSms(),
										cidade.getPermiteMktEmail());
		
		return dto;
	}
}
